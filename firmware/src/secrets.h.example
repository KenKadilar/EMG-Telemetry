// Template. Copy this file to secrets.h and fill in your own values. secrets.h is gitignored.
#ifndef SECRETS_H
#define SECRETS_H

const char* wifiSsid = "YourWifiName";        // must be a 2.4 GHz network
const char* wifiPassword = "YourWifiPassword";

const char* mqttBrokerHost = "your-broker-hostname"; // the broker machine's mDNS name (its hostname; .local is added for you)
const int mqttBrokerPort = 1883;

#endif
